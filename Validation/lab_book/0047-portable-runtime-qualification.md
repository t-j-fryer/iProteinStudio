# 0047 — Portable runtime and PSICHIC integration qualification

20–22 September2026. [Project0177](../../lab_book/0177-integrate-psichic-portable-runtimes.md).

Hypothesis: exact installed engine closures can run after relocation without client compilation, with scientific outputs inside declared equivalence gates. Host: Apple M4 Max,64 GB, macOS26.6.1. Fresh-Mac testing explicitly deferred. No biological-binding or throughput claim.

**14/14 paired checks passed**, spanning Boltz2, IntelliFold Flash/full, Protenix v2/Constraint, OpenFold3, RFD3, AntiFold, NESSO and five sequence-model modes. Paired fixtures/settings, package identities, output metrics and retained failed attempts are recorded in the [qualification JSON](../experiments/portable_runtimes_v1/release_qualification.json) and project0177. PSICHIC separately passes64 production GPU/CPU scores against CPU references, maximum absolute error0.000396937 below0.001; completed resume performs no inference.

Final12 archive identities exclude learned weights and total2,938,147,290 bytes compressed. Eight required LASErMPNN `.pt` chemical-table/dataset-ID files are explicitly classified as static data, not learned parameters; repaired package was retested. Python/Finder metadata-only final changes preserve scientific code. GitHub asset SHA-256 and sizes match local archives. Current-Mac final installation completed all components successfully under managed job `job-c7c2fedd9eac`.

Acceptance uses fixed seeds, production steps/recycles/guidance, explicit empty MSA for ubiquitin76 and immutable raw outputs. Initial multiprocessing-launcher, audit-reader and omitted-static-table failures were diagnosed and retained; only corrected reruns establish acceptance. Raw audit: `output/portable_runtimes_v1/analysis/FINAL_SCIENTIFIC_AUDIT.json`. These elapsed times are diagnostic because packaging/builds overlapped, not a performance experiment.

Native minimums26.0/26.2 are enforced by UI and installer;26.6.1 qualifies. No claim of fresh-Mac, other-chip or notarization acceptance. Publication and final installed-bridge receipts are recorded in project0177.

App0.2.1 build44 and all12 runtime archives are published. GitHub checksum/size and Sparkle signature verification passed; empty-root public control/MPNN installation passed without invoking blocked developer tools. Additional final installed full-model checks encountered a shared macOS MPSGraph scratch-folder stall and are not passes. Cache repair awaits explicit user approval after automatic approval review rejected the shared-directory operation. See project0177 for the exact limit and recorded attempts.

Follow-up: user-authorized repair and additional local GPU checks completed in [0048](0048-mps-scratch-recovery.md). Both previous/published Boltz runtimes and repeated PSICHIC scoring passed after preserving/replacing the affected temporary directory. Version0.2.2 adds a bounded storage health check; scientific runtime archives are unchanged.
